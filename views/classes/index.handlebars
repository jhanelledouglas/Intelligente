<h2>Departments</h2>
{{#each classes}}
	<div class="block">
		<h3>{{title}}</h3>
		<p>
			{{description}}
		</p>
		<p>Instructor: <strong>{{instructor}}</strong></p>
		<a class="button" href="/classes/{{_id}}/details">Department Details</a>
	</div>
{{/each}}